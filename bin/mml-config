#!/usr/bin/env bash
# set up python environment
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
ROOT="$(dirname "$DIR")"
export PYTHONPATH="$ROOT"
PYFILE="$ROOT/core/configurer.py"
PYTHON=$(which python)
$PYTHON $PYFILE "$@"
